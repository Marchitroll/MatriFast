/**
 * Enumeración de los roles de usuario permitidos en el sistema.
 */
const ROLES_USUARIO = Object.freeze({
    DOCENTE: 'Docente',
    REPRESENTANTE_LEGAL: 'Representante legal'
});

export default ROLES_USUARIO;
